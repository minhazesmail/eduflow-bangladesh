import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'

export default async function ReportsPage(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const{data:m}=await s.from('flat_members').select('flat_id').eq('user_id',user.id).eq('status','active').maybeSingle();if(!m)redirect('/onboarding');const{data:cycles}=await s.from('cycles').select('id,start_date,end_date,status,settlements(user_id,total_meals,meal_cost,total_contribution,balance,opening_balance)').eq('flat_id',m.flat_id).order('start_date',{ascending:false});return <div className="space-y-6"><div><h1 className="text-2xl font-bold">Reports</h1><p className="text-sm text-slate-500">Closed cycles are immutable settlement snapshots.</p></div>{(cycles??[]).filter(c=>c.status==='closed').map(c=><section key={c.id} className="card"><div className="mb-4 flex items-center justify-between"><div><h2 className="font-semibold">{c.start_date} → {c.end_date}</h2><p className="text-sm text-slate-500">Closed cycle</p></div><a className="btn-secondary" href={`/api/reports/settlement?cycleId=${c.id}`}>Export summary</a></div><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-sm"><thead><tr className="border-b text-left text-slate-500"><th className="py-2">Member</th><th>Meals</th><th>Meal cost</th><th>Contribution</th><th>Balance</th></tr></thead><tbody>{(c.settlements??[]).map((r:{user_id:string;total_meals:number;meal_cost:number;total_contribution:number;balance:number})=><tr key={r.user_id} className="border-b last:border-0"><td className="py-2">{r.user_id===user.id?'You':'Member'}</td><td>{r.total_meals}</td><td>৳{Number(r.meal_cost).toFixed(2)}</td><td>৳{Number(r.total_contribution).toFixed(2)}</td><td className={Number(r.balance)<0?'text-red-600':'text-green-700'}>৳{Number(r.balance).toFixed(2)}</td></tr>)}</tbody></table></div></section>)}</div>}
