import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { ExpenseForm } from '@/components/forms'

export default async function ExpensesPage(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const{data:m}=await s.from('flat_members').select('flat_id,role').eq('user_id',user.id).eq('status','active').maybeSingle();if(!m)redirect('/onboarding');const{data:c}=await s.from('cycles').select('*').eq('flat_id',m.flat_id).eq('status','open').order('start_date',{ascending:false}).limit(1).single();if(!c)return <div className="card">No open cycle.</div>;const{data:expenses}=await s.from('expenses').select('*').eq('cycle_id',c.id).order('created_at',{ascending:false});const total=(expenses??[]).reduce((sum,e)=>sum+Number(e.amount),0);return <div className="space-y-6"><div><h1 className="text-2xl font-bold">Expenses</h1><p className="text-sm text-slate-500">Food cost uses grocery only; other shared costs stay separate.</p></div><div className="grid gap-4 lg:grid-cols-[360px_1fr]"><ExpenseForm flatId={m.flat_id} cycleId={c.id}/><section className="card"><div className="mb-4 flex items-center justify-between"><h2 className="font-semibold">Current cycle</h2><span className="font-bold">৳{total.toFixed(2)}</span></div><div className="space-y-3">{(expenses??[]).map(e=><div key={e.id} className="flex items-center justify-between border-b pb-3 text-sm"><div><div className="font-medium capitalize">{String(e.category).replace('_',' ')}</div><div className="text-slate-500">{e.note||'—'}</div></div><div className="font-semibold">৳{Number(e.amount).toFixed(2)}</div></div>)}</div></section></div></div>}
